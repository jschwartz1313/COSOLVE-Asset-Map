databases:
  - name: cosolve-asset-map-db
    # Evaluation only: Render free PostgreSQL expires after 30 days and has no backups.
    plan: free
    databaseName: cosolve_asset_map
    user: cosolve_asset_map

services:
  - type: web
    name: cosolve-asset-map
    runtime: python
    plan: free
    buildCommand: ./build.sh
    startCommand: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
    healthCheckPath: /health/
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_ALLOWED_HOSTS
        value: .onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: cosolve-asset-map-db
          property: connectionString
      - key: REQUIRE_SITE_LOGIN
        value: "true"
      - key: DJANGO_SUPERUSER_USERNAME
        sync: false
      - key: DJANGO_SUPERUSER_EMAIL
        sync: false
      - key: DJANGO_SUPERUSER_PASSWORD
        sync: false
      - key: PYTHON_VERSION
        value: 3.12.7
