{
  "name": "cosolve-asset-map-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/frontend/*.test.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
